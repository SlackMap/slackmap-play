import { Tracker } from './tracker';


describe('GremlinsComponent', () => {
    let tracker;
    beforeEach(() => {
      tracker = new Tracker();
    });

    it('should create', () => {
      expect(tracker).toBeTruthy();
    });
  });

